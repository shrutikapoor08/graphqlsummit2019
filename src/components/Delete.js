import React  from "react";


const Delete = (props) => {
  return (
    <button type="submit" onClick={props.onClick} className="mui-btn mui-btn--fab">
      -
    </button>
  )

};

export default Delete
